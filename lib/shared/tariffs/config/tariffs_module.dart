import 'package:get_it/get_it.dart';

class TariffsModule {
  static void configure(GetIt getIt) {
    // Все зависимости (Repository, Use Cases, BLoC) регистрируются автоматически через @injectable
    // Этот модуль больше не нужен, но оставляем для совместимости
  }
}
