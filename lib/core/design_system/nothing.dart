import 'package:flutter/material.dart';

class BeNothing extends StatelessWidget {
  const BeNothing({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
